import { SumArrayTablePipe } from './sum-array-table.pipe';

describe('SumArrayTablePipe', () => {
  it('create an instance', () => {
    const pipe = new SumArrayTablePipe();
    expect(pipe).toBeTruthy();
  });
});
