import { DropdownSidemenuDirective } from './dropdown-sidemenu.directive';

describe('DropdownSidemenuDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownSidemenuDirective();
    expect(directive).toBeTruthy();
  });
});
