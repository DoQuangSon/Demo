import { CheckNumberOnlyDirective } from './check-number-only.directive';

describe('CheckNumberOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckNumberOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
