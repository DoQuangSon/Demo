import { CheckPhoneNumberDirective } from './check-phone-number.directive';

describe('CheckPhoneNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckPhoneNumberDirective();
    expect(directive).toBeTruthy();
  });
});
